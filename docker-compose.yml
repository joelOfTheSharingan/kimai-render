services:
  kimai:
    image: kimai/kimai2:apache
    container_name: kimai
    ports:
      - "8001:8001"
    environment:
      APP_SECRET: password123

      DATABASE_URL: postgresql://postgres.vlgvcgioikaxrvksbttp:KimaiDatabase2026@aws-1-ap-northeast-2.pooler.supabase.com:5432/postgres?sslmode=require

    restart: unless-stopped
